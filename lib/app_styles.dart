import 'package:flutter/material.dart';

// App-wide text styles used by the example app. Keep these as const so they
// can be used in `const` widget constructors (e.g. `const Text(..., style: normalText)`).
const TextStyle heading1 = TextStyle(
  fontSize: 20.0,
  fontWeight: FontWeight.bold,
);

const TextStyle normalText = TextStyle(
  fontSize: 16.0,
);

// TODO Implement this library.
